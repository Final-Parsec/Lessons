using System;

namespace Lesson8
{
    class StringManipulation
    {
        static void Main(string[] args)
        {
            // b
            // 2
            // !
            // Ñ

            //char myFirstChar = '@';
            //Console.WriteLine("Char: " + myFirstChar + " | Int: " + (int)myFirstChar);

            //for (int x = 1; x <= 126; x++)
            //{
            //    char numberToChar = (char)x;
            //    Console.WriteLine($"count {x} : {numberToChar}"); // Show ascii table after this
            //}


            string alphabet = "abcd";
            //for (int index = 0; index < alphabet.Length; index++)
            //{
            //    //string letter = alphabet[index]; // Doesn't work
            //    char letter = alphabet[index];
            //    Console.WriteLine($"char at index {index}: {letter}");
            //}

            // Index out of bounds rant
            //string reverse = "";
            //for (int index = alphabet.Length - 1; index >= 0; index--)
            //{
            //    //string letter = alphabet[index]; // Doesn't work
            //    char letter = alphabet[index];
            //    Console.WriteLine($"char at index {index}: {letter}");
            //    reverse += letter;
            //}

            //Console.WriteLine(reverse);



            //string foods = "pizza, rice, pasta, bread";



        }
    }
}
